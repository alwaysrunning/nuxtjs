// @ts-ignore
const state = () => ({
  cmsConfig: {}
})

const actions = {
  // async nuxtServerInit({ commit, dispatch }, { req, store }) {
  //   if (req.session) {
  //   }
  //   await dispatch('app/getCMSConfig')
  // }
}

export default {
  state,
  actions
}
